spring:
  application:
    name: demo

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/demo_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 826350
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml

seniverse:
  api:
    key: ${SENIVERSE_API_KEY}

llm:
  api-key: ${A_BAILIAN_API_KEY}
  model: qwen-plus
  base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
  max-history: 20
  system-prompt: 你是阿里云通义千问大模型（Qwen），作为 MyDemo Bot 微信助手。你友好、简洁，能回答问题、闲聊、讲笑话。回复要简短，适合微信聊天。当用户问天气、系统状态、版本、帮助时，使用提供的工具来查询。如果用户只是在聊天，直接友好回复即可。

logging:
  level:
    com.Myself.demo: DEBUG