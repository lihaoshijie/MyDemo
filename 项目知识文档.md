# MyDemo Bot 项目周报

## 一、项目概述

基于 Spring Boot + MyBatis 框架，集成 iLink SDK 和 DashScope SDK，实现了一个功能完善的微信智能助手 Bot。用户通过微信聊天即可与 AI 对话、查询天气、识别图片、生成图片、收听语音播报等。

---

## 二、技术栈

| 层级 | 技术 | 核心类 |
|------|------|--------|
| 框架 | Spring Boot 4.0.7 + MyBatis | DemoApplication, TUserMapper |
| 微信接入 | wechat-ilink-sdk-java v2.3.3 | ILinkClient, WeixinMessage, MessageItem |
| 大模型 | dashscope-sdk-java v2.22.25 | Generation, MultiModalConversation, ImageGeneration |
| 对话存储 | Redis (spring-boot-starter-data-redis) | StringRedisTemplate |
| 天气 | 心知天气 API | WeatherService (WebClient) |
| 语音合成 | DashScope TTS | HttpSpeechSynthesizer |
| 数据库 | MySQL | TUserMapper |

---

### 两个 SDK 的区别与作用

| | wechat-ilink-sdk-java | dashscope-sdk-java |
|--|----------------------|-------------------|
| **开发者** | 社区开发者 lith0924 | 阿里官方 |
| **用途** | 微信 Bot 接入 | 阿里云千问大模型调用 |
| **作用** | 连接微信，收发消息 | 调用 AI 模型，理解/生成内容 |

```
用户微信发消息
  ↓
iLink SDK（通信层）：扫码登录、接收消息、发送文字/图片/文件/语音
  ↓
DashScope SDK（AI层）：理解消息、查询天气、识图、生图、合成语音、Token估算
  ↓
iLink SDK（通信层）：把回复发回给用户微信
```

**具体分工**：

| 功能 | 使用的 SDK |
|------|-----------|
| 扫码登录 / 会话恢复 | iLink |
| 收微信消息 | iLink |
| 发文字 / 图片 / 文件 / 语音 | iLink |
| 理解用户意图 + 对话 (qwen-plus) | DashScope |
| 识别图片内容 (qwen-vl-max) | DashScope |
| 生成图片 (wan2.6-t2i / wan2.6-image) | DashScope |
| 语音合成 (cosyvoice-v3-flash) | DashScope |
| Token 估算 (Tokenizer) | DashScope |
| 查询天气 | 心知天气 API（第三方） |

**一句话总结**：iLink SDK 管"收发"，DashScope SDK 管"思考"。

---

### 项目类说明

| 类 | 所属包 | 作用 |
|----|--------|------|
| DemoApplication | — | 启动入口，AppRunner 触发 WeChatBotService + CommandExecutor |
| WeChatBotService | bot | iLink 客户端生命周期、消息收发、图片/语音检测、语音模式开关 |
| CommandRouter | bot | 消息路由：直接命令匹配 → LLM Function Calling → 回退聊天 |
| SessionManager | bot | LoginContext 序列化/反序列化，扫码免登录 |
| CommandExecutor | cli | CLI 交互循环，读取键盘输入 → 匹配命令 → 执行 |
| Command | command | 命令接口，CLI 和 Bot 共用 |
| WeatherCommand | command | 查天气（实时 + 3/7/15天预报） |
| HelpCommand | command | 显示帮助 |
| StatusCommand | command | 系统状态（运行时间、内存） |
| VersionCommand | command | 版本信息 |
| ExitCommand | command | 仅限 CLI |
| WeatherService | service | 调心知天气 API，解析实时/预报数据 |
| LlmService | service | DashScope SDK 对话、Function Calling、记忆加载 |
| ChatService | service | Redis + Token阈值 + 滑动窗口 + 摘要压缩管理对话历史 |
| ImageService | service | 识图(qwen-vl-max)、文生图(wan2.6-t2i)、图生图(wan2.6-image) |
| VoiceService | service | TTS 语音合成（cosyvoice-v3-flash MP3） |
| MemoryService | service | 用户记忆持久化（ConcurrentHashMap + JSON 文件） |
| WeatherResponse | model | 天气数据模型（温度/体感/湿度/风向等） |
| TUser | entity | 数据库用户实体 |
| TUserMapper | mapper | MyBatis 数据访问 |
| HelloController | controller | REST 接口（GET /hello, GET /user/list） |
| BusinessException | exception | 业务异常基类，5个子类 |
| MarkdownUtil | util | Markdown → 纯文本，TTS 播报去格式 |
| JsonUtil | util | Jackson 一行式 JSON 序列化/反序列化 |
| JsonSchemaBuilder | util | 流式构建 JSON Schema（替代手写 JsonObject） |
| ToolBuilder | util | 封装 JsonSchemaBuilder → FunctionDefinition → ToolFunction |
| MediaExtractor | util | 统一提取 MessageItem（图片/语音/文件/文字） |
| SvgUtil | util | SVG 校验/提取/转 PNG（Batik） |

---

### 工具类详解

**MarkdownUtil** — Markdown → 纯文本清洗，用于 TTS 语音播报

去格式：标题 `###`、加粗 `**`、斜体 `*`、删除线 `~~`、代码块 ` ``` `、链接 `[]()`、列表符号、表格分隔线。换行转为中文逗号。

```
输入: "## 总结\n- 杭州**天气**：28°C"
输出: "总结，杭州天气：28°C"
```

**JsonUtil** — Jackson ObjectMapper 包装，一行式 JSON 操作

`toJson(obj)` / `fromJson(json, Class)` / `fromJson(json, TypeReference)` / `prettyPrint(obj)`

替代 MemoryService、SessionManager 中的 `new ObjectMapper()` 样板代码。

**JsonSchemaBuilder** — 流式构建工具定义的 JSON Schema，不再手写 JsonObject

```java
new JsonSchemaBuilder()
    .addString("city", "城市名称", true)   // required=true
    .addEnum("days", "天数", 1, 3, 7, 15) // 枚举值
    .build();
```

在 LlmService 中替代了 `buildWeatherParams/buildImageGenParams/buildMemoryParams` 三个方法里手动拼装 `JsonObject` 的 15 行代码。

**ToolBuilder** — 封装 JsonSchemaBuilder 生成 FunctionDefinition + ToolFunction，一步到 ToolFunction

```java
// 带参数的工具
new ToolBuilder("weather", "查询天气")
    .addString("city", "城市名称", true)
    .build();

// 无参数的工具
ToolBuilder.simple("help", "显示帮助");
```

在 CommandRouter.buildTools() 中替代了手动嵌套 `ToolFunction.builder().function(FunctionDefinition.builder()...)` 的重复写法。

**MediaExtractor** — 从 WeixinMessage 中统一提取各种媒体类型

不再需要 `extractImage/extractVoice/extractFile/extractText` 四个分散方法：

```java
MediaExtractor m = new MediaExtractor(msg);
if (m.hasImage()) handleImage(m.getImage(), m.getText());
if (m.hasVoice()) handleVoice(m.getVoice());
if (m.hasFile())  handleFile(m.getFile(), m.getText());
String text = m.getText();
```

**SvgUtil** — SVG 校验、提取、转 PNG（基于 Batik 渲染引擎）

`isValid(svg)` — 检测是否完整 SVG + 无脚本注入
`extract(text)` — 从混合文本中提取 `<svg>...</svg>`
`toPngBytes(svg)` — SVG → PNG bytes（宽度 800px），用于生成图表发送

---

## 三、功能模块

### 3.1 微信接入模块

| 功能 | 实现方式 |
|------|---------|
| Bot 扫码登录 | ILinkClient.executeLogin() |
| 会话持久化 | SessionManager 序列化 LoginContext 到 JSON，重启免扫码 |
| 消息接收 | HeartbeatService 自动轮询 + OnMessageListener 回调 |
| 文字发送 | client.sendText() |
| 图片发送 | client.sendImage() |
| 语音发送 | client.sendVoice() |

**解决的问题**：
- 每次启动需要扫码，体验差 → 实现会话持久化，自动登录，3秒内可按Enter切换账号

### 3.2 大模型对话模块

| 功能 | 实现方式 | 模型 |
|------|---------|------|
| 文字对话 | Generation.call() | qwen-plus |
| Function Calling | 定义 weather/generate_image/help/status/version 工具 | qwen-plus |
| 识图 | MultiModalConversation.call() | qwen-vl-max |
| 文生图 | ImageGeneration.call() | wan2.6-t2i |
| 图生图 | ImageGeneration.call()（多图输入） | wan2.6-image |
| 语音合成 | HttpSpeechSynthesizer.callAndReturnAudio() | cosyvoice-v3-flash |

**关键技术**：
- 使用 DashScope SDK 替代手动 WebClient，自动处理序列化、异常、API Key
- Function Calling 实现一次 API 调用完成意图判断+执行，比两阶段调用减少一半延迟

### 3.3 天气查询模块

| 功能 | API | 说明 |
|------|-----|------|
| 实时天气 | /v3/weather/now.json | 温度、体感、湿度、风向、风速、能见度、气压 |
| 多天预报 | /v3/weather/daily.json | 3/7/15天，LLM 根据语义自动判断天数 |
| 全球城市 | 同上 | 支持北京、纽约、东京、伦敦等全球城市 |

**智能分流**：实时/未来天气 → 调心知天气 API；主观感受/历史天气 → 千问直接回答

**解决的问题**：
- 自然语言"杭州未来五天"无法被正则匹配 → 改用 LLM Function Calling，千问自行提取城市和天数
- "纽约的天气"查询失败 → Tool 描述添加全球城市说明
- 天气输出太简陋 → 增加体感温度、风速、能见度、气压字段

### 3.4 图片处理模块

#### 图片识别

用户发送图片 → 自动识图（qwen-vl-max）→ 返回文字描述 → 支持多轮追问

**架构**：
```
图片到达 → ImageService.recognizeImage() → 存描述到 imageDesc
文字提问 → 从 imageDesc 取描述 + 用户问题 → LLM 基于描述回答（不重复调识图API）
用户: "重新看看" → 重新调识图API，更新描述
无关话题 → 自动清除缓存
```

#### 图片生成

用户说"画一只猫" → LLM Function Calling → wan2.6-t2i 文生图 → 回复图片

#### 图片变换

用户说"变成卡通/把电脑p掉" → 检测为变换请求 → wan2.6-image 图生图（原图+风格指令）→ 回复变换后图片

#### 多图支持

用户发送多张图片 → 缓存最多5张 → "合并这两张图" → 多图传给 wan2.6-image → 生成合并图

**数据结构**：
```
ImageEntry { bytes, desc, timestamp }
ConcurrentHashMap<String, List<ImageEntry>> imageBuffers  // 每用户最多5张，5分钟过期
```

**解决的问题**：
- 微信图片和文字分开发送 → 图片缓冲机制，文字到达时合并处理
- 只发图片不发文字导致后续聊天被强制识图 → 5分钟TTL超时清除
- 图片描述太长占用微信显示 → 短描述输出给用户，完整描述内部存储
- 图生图没参考原图 → 区分变换/从零生成，变换时走 wan2.6-image 传原图
- keyword 匹配意图不准确 → 去掉 isImageQuestion/isImageGenRequest，改用 LLM 上下文感知

### 3.5 记忆模块

**功能**：让 Bot 记住用户的个人信息（名字、生日、喜好等），重启后不丢失。

| 功能 | 实现方式 |
|------|---------|
| 记忆保存 | CommandRouter 添加 `remember_fact` 工具，LLM Function Calling 触发保存 |
| 记忆读取 | LlmService 每次调用前从 MemoryService 读取，拼接到 system prompt |
| 持久化 | 文件存储：`memory-{userId}.json`，Jackson 序列化/反序列化 |
| 数据结构 | `ConcurrentHashMap<String, ConcurrentHashMap<String, String>>` |

**流程**：
```
用户: "叫我托尼"
  → LLM 判断是个人信息 → 调 remember_fact({"key":"名字","value":"托尼"})
  → MemoryService.setFact(userId, "名字", "托尼")
  → 写入 memory-o9cq80x.json

每次对话时:
  LlmService.buildSystemPrompt(userId)
  → 从 memory-{userId}.json 读取事实列表
  → 拼接到 system prompt: "用户信息：名字是托尼，生日是5月20号..."
  → 千问看到用户信息后回答问题时会自动使用
```

**记忆文件格式**（`memory-o9cq80xWz8OEdKTapGagHOZB5cs4@im.wechat.json`）：
```json
{"名字": "托尼", "生日": "5月20号", "职业": "程序员"}
```

### 3.6 语音模块

**语音收发完整链路**：

| 功能 | 状态 | 实现方式 |
|------|------|---------|
| 收微信语音气泡 | ✅ | extractVoice() 检测 MessageItem.getVoice_item() |
| 语音转文字（识别） | ✅ | VoiceItem.getText() — iLink 服务端自动转写，无需集成额外 ASR |
| LLM 处理语音内容 | ✅ | 转写文本 → CommandRouter.route() — 与文字消息走同一流程 |
| 文字转语音（合成） | ✅ | VoiceService.textToSpeech() → HttpSpeechSynthesizer → cosyvoice-v3-flash → WAV |
| 发语音文件 | ✅ | client.sendFile() → 微信文件消息 → 点开即播 |
| 语音模式开关 | ✅ | "开启语音"→voiceMode=true → 回复自动附带 TTS；"关闭语音"→移除标记 |

**为什么不用语音气泡**：

微信语音气泡仅支持 SILK 格式，WAV/MP3 发了也不显示气泡。且触碰微信语音协议有封号风险。

改用 `client.sendFile()` 以文件形式发送 MP3 语音，用户点开文件就能播放，不走微信语音通道，无封号风险。

**格式对比**：

| | 语音气泡 (sendVoice) | 语音文件 (sendFile) |
|--|---------------------|-------------------|
| 格式要求 | 仅 SILK | 任意格式（当前用MP3） |
| 封号风险 | 高 | 无 |
| 播放方式 | 自动出现气泡 | 点开文件播放 |
| SDK 方法 | client.sendVoice() | client.sendFile() |

**SDK 依赖**：

| 功能 | SDK | 类/方法 |
|------|-----|---------|
| 语音转写（收） | iLink SDK | VoiceItem.getText() |
| 语音合成（发） | DashScope SDK | HttpSpeechSynthesizer.callAndReturnAudio() |
| 语音发送 | iLink SDK | ILinkClient.sendFile() |

**音色切换功能**：

语音合成支持切换不同音色（发音人），基于 `cosyvoice-v3-flash` 模型。

| 名称 | voice 参数 | 描述 |
|------|-----------|------|
| 默认男声 | longanyang | 阳光大男孩，20-30岁，中英文 |
| 女声元气 | longanhuan_v3 | 欢脱元气女，20-30岁，中英文+方言 |
| 女声欢脱 | longanhuan | 欢脱女，20-30岁，中英文 |

**切换方式**：

| 方式 | 示例 | 实现 |
|------|------|------|
| 自然语言前缀匹配 | `切换女声`、`换成元气`、`用女声欢脱` | `WeChatBotService.matchVoiceSwitch()` → `voicePref[userId]` → `textToSpeechMp3(text, code)` |

**SDK 支持**：`HttpSpeechSynthesisParam.voice("longanhuan_v3")` 切换音色，模型 `cosyvoice-v3-flash`

**持久化**：`ConcurrentHashMap<String, String> voicePref` 内存存储，重启丢失

---

### 3.8 文件收发模块

**功能**：接收用户发送的文件（txt/md/csv/json/代码/Office），读取内容后交给 LLM 处理，支持生成总结文件返回。

**流程**：

```
用户发文件 + "总结这个文档"
  → extractFile() 检测 FileItem
  → downloadFileFromMessageItem() 下载字节
  → extractTextContent() 提取文本（UTF-8/GBK）
    ├── 成功 → 文本内容传给 LLM → 回答
    └── 失败 → OSSUtils.upload() 上传OSS → URL传给LLM
  → 用户说"总结/导出" → 生成 原文件名_总结.txt 发回
```

**支持的文件类型**：

| 类别 | 扩展名 | 读取方式 |
|------|--------|---------|
| 代码 | .java .py .js .ts .c .cpp .h .sql .sh .bat | UTF-8/GBK 文本提取 |
| 数据 | .json .csv .xml .yaml .yml | 同上 |
| 文档 | .txt .md .html .log | 同上 |
| PDF | .pdf | PDFBox 提取 |
| Word | .docx | Apache POI (XWPFWordExtractor) |
| Excel | .xlsx | Apache POI (XSSFWorkbook) |
| 老格式 | .doc .xls .ppt .pptx | 文本尝试+OSS回退 |
| 配置 | .ini .cfg .conf | 同上 |

**依赖库**：

| 库 | 用途 |
|----|------|
| Apache POI (poi-ooxml 5.3.0) | Word/Excel 文档解析 |
| Apache PDFBox (3.0.3) | PDF 文本提取 |
| OSSUtils | 文件上传OSS（二进制文件回退方案） |

**SDK 依赖**：

| 功能 | SDK | 方法 |
|------|-----|------|
| 收文件 | iLink SDK | downloadFileFromMessageItem() |
| 发文件 | iLink SDK | sendFile() |
| 上传OSS | DashScope SDK | OSSUtils.upload() |

### 3.9 对话上下文压缩（历史摘要）

**背景**：长对话 token 消耗大，旧消息直接丢弃则丢失上下文。需要多层机制管理。

**当前实现**：四层架构

| 层 | 机制 | 说明 |
|----|------|------|
| 1 | Redis 持久化 | `chat:{userId}` → JSON，24h TTL 自动过期沉睡用户 |
| 2 | Token 阈值判断 | DashScope Tokenizer 精确估算，超 100K 触发压缩 |
| 3 | 滑动窗口 | 消息数超 50 条时触发压缩（兜底防线） |
| 4 | 对话摘要 | LLM 将旧消息压缩为 80 字摘要，保留为 system 消息 |

**压缩流程**：

```
触发条件: tokens > 100K 或 消息数 > 50

流程:
  1. 取最旧的一半消息
  2. 拼接为文本 → 调 LLM 生成 80 字以内摘要
  3. 替换为一条 system 消息: {"role":"system","content":"历史摘要：xxx"}
  4. 保留最近 10 条完整消息 + 新摘要
  5. 保存回 Redis，刷新 24h TTL

示例:
  对话: "用户：杭州天气.. 助手：杭州28°... 用户：画只猫.. 助手：生成成功.."
  摘要: "历史摘要：用户查了杭州天气(28°多云)，让画了猫"
```

**优势**：
- Redis 自动清理沉睡用户，零维护
- Token 精确估算，不浪费模型窗口
- 双重阈值互备，异常场景不丢失上下文
- 摘要压缩保留核心信息，丢弃冗余原文

## 四、项目架构

```
WeChatBotService（消息入口）
  ├── 图片 → ImageService.recognizeImage() / generateImage() / transformImage()
  ├── 语音 → voiceItem.getText() → 正常流程
  └── 文字 → CommandRouter.route()
              ├── 直接命令 → Command.execute()
              └── LLM Function Calling → LlmService.chat()
                    ├── weather → WeatherService → 心知天气
                    ├── generate_image → ImageService → 生图
                    ├── help/status/version → 命令执行
                    └── text → ChatService 对话历史 → 千问回复
```

---

## 五、解决的问题汇总

| 序号 | 问题 | 解决方案 |
|------|------|---------|
| 1 | 每次启动需扫码 | SessionManager 持久化 LoginContext |
| 2 | 微信消息格式丑陋 | 双换行 + 紧凑格式 |
| 3 | 自然语言无法触发命令 | LLM Function Calling |
| 4 | 天气不支持多天/全球 | 新增 daily API + Tool 描述更新 |
| 5 | 图片文字分开发送 | 图片缓冲机制 |
| 6 | 图生图不参考原图 | wan2.6-image 替代 wan2.6-t2i |
| 7 | 关键词意图不准确 | 去掉关键词，改用 LLM 上下文感知 |
| 8 | 生图无反馈等待久 | 先生成中提示 + 调用 |
| 9 | 图片描述展示太长 | 短描述展示 + 完整描述存储 |
| 10 | 图片生成提示词泄露 | sendImage caption 置空 |

---

## 六、收获与理解

1. **Function Calling 替代关键词匹配**：大模型的语义理解能力远强于规则匹配，将意图判断交给 LLM 是更优方案
2. **SDK 优先于手动 HTTP**：DashScope SDK 自动处理序列化、异常、重试，开发效率和数据可靠性显著提升
3. **协议层面的局限与应对**：微信图片文字分离发送是协议限制，通过缓存+TTL机制优雅解决
4. **上下文管理**：图片描述缓存、多轮对话历史、语音模式开关，都是上下文管理的典型案例
5. **多模态能力整合**：文字对话(qwen-plus) + 识图(qwen-vl-max) + 文生图(wan2.6-t2i) + 图生图(wan2.6-image) + 语音(cosyvoice-v3-flash)，实现完整的多模态交互闭环
6. **四层上下文管理**：Redis + Token阈值 + 滑动窗口 + 摘要压缩，每一层解决一个维度的问题，堆叠起来应对真实对话场景
7. **Tool 描述精细度决定 LLM 行为**：`generate_image` 和 `transform_image` 拆开描述后，LLM 自动理解了"从零生成"和"基于原图变换"的区别

---

## 七、代码统计

| 指标 | 数量 |
|------|------|
| Java 源文件 | 29 个 |
| 服务层类 | 7 个（Weather / Llm / Chat / Image / Voice / Memory / Session） |
| 命令实现 | 5 个（Help / Weather / Status / Version / Exit） |
| 外部 API 集成 | 心知天气 + 千问6个模型（含 Tokenizer） |
| SDK 依赖 | wechat-ilink-sdk + dashscope-sdk-java + spring-boot-starter-data-redis |

---

## 八、核心技术机制详解

### 1. 登录免扫码机制如何实现

**核心思路**：利用 SDK 的 LoginContext 序列化能力，首次扫码后保存登录凭证，后续启动直接恢复。

```
首次启动:
  executeLogin() → 用户扫码 → onLoginSuccess(LoginContext ctx)
  → SessionManager.save(ctx)
  → 序列化为 ilink-session.json:
  {
    "botToken": "xxx",
    "userId": "xxx@im.bot",
    "botId": "xxx",
    "baseUrl": "https://..."
  }

后续启动:
  SessionManager.load() → 反序列化 LoginContext
  → ILinkClient.builder().loginContext(ctx) → SDK 内置恢复登录态
  → HeartbeatService 直接启动 → 消息监听生效

切换账号:
  启动时 3 秒倒计时 → 按 Enter → SessionManager.clear() → 走扫码流程
```

**关键点**：LoginContext 是 SDK 内部的登录凭证，SDK 通过 `loginContext()` 方法接收后直接进入 LOGGED_IN 状态。不需要 token 刷新，由 SDK 内部的 HeartbeatService 自动维持连接。

---

### 2. 图像与文字结合如何实现

**问题**：微信协议将图片和文字拆成两条独立的 WeixinMessage 推送，存在时序问题（文字先到、图片后到）。

**解决**：图片缓冲 + 描述拼接方案。

```
数据结构:
  ConcurrentHashMap<String, List<ImageEntry>> imageBuffers
  ImageEntry { byte[] bytes; String desc; long timestamp; }
  MAX_IMAGES = 5, IMAGE_BUFFER_TTL = 5分钟

流程:
  用户发图 → handleImageMessage()
    → downloadImage() → recognizeImage() 获取描述
    → addImageToBuffer(userId, bytes, desc)
    → 回复短摘要

  用户发"这是什么" → handleMessage()
    → 检查 imageBuffers 有缓存
    → buildImageContext(images, text):
      "用户发送了图片，内容：{完整描述}\n\n用户说：这是什么"
    → CommandRouter.route() → LLM 看到图片描述 + 问题 → 基于描述回答
```

**关键点**：图片描述作为上下文拼接进用户消息，不是直接发图片给 LLM。LLM 通过文字描述理解图片内容，实现"看过图片"的效果。无需重复调识图 API。

---

### 3. 图生图如何实现

**核心思路**：有原图缓存时自动使用 wan2.6-image 模型，SDK 自动上传临时文件作为参考图。

```
sendReply() 检测到 IMG_GEN:
  ├── imageBuffers 有数据
  │     → ImageService.transformImage(List<byte[]> images, String prompt)
  │     → 写临时文件 → content = [
  │           {"image": "/tmp/transform_1.png"},
  │           {"image": "/tmp/transform_2.png"},
  │           {"text": "把这张图变成卡通风格"}
  │        ]
  │     → ImageGeneration.call() → wan2.6-image 模型
  │     → 模型接收原图 + 风格指令 → 生成基于原图的变换图
  │     → 删除临时文件
  │
  └── 无数据 → ImageService.generateImage(prompt)
        → content = [{"text": "一只猫"}]
        → ImageGeneration.call() → wan2.6-t2i 模型
        → 从零生成
```

**关键点**：wan2.6-image 支持 image+text 混合输入，模型自行判断原图是否相关。不需要代码层面区分"变换"和"从零生成"，有缓存就传原图，模型自己决定是否参考。

---

### 4. 用户发图后再发送内容如何判断意图

**核心思路**：不判断。去掉所有关键词匹配（isImageQuestion / isImageGenRequest），用 LLM 上下文感知替代。

```
图片缓存存在 → buildImageContext() 拼接描述 + 用户文字 → CommandRouter.route()
  ↓
LlmService.chat() 带 tools（weather / generate_image / help / status / version）
  ↓
千问根据语义自行判断:
  ├── "这是什么"       → 图片相关问题 → 基于描述文字回答
  ├── "画一只猫"       → 生图请求     → function_call("generate_image")
  ├── "变成卡通"       → 编辑请求     → function_call("generate_image")
  ├── "天气"           → 命令         → function_call("weather") → 同时清除图片缓存
  ├── "今天星期几"     → 闲聊         → 文字回复
  └── "我没说查北京"   → 纠正         → LLM 重新理解回答
```

**命令保护**：如果用户文字匹配到内置命令（如 weather），直接清除图片缓存并执行命令，避免图片上下文干扰命令执行。

---

### 5. 天气生成机制

**核心思路**：LLM Function Calling + 心知天气 API，支持实时和预报。

```
实时天气:
  用户: "杭州天气" → CommandRouter → 未匹配命令
  → LlmService.chat() + tools → 千问判断语义
  → function_call("weather", {"city":"杭州", "days":1})
  → WeatherCommand.execute(["杭州"])
  → WeatherService.getWeather("杭州")
  → GET /v3/weather/now.json?location=杭州
  → 解析: text, temperature, feels_like, humidity, wind_direction, wind_speed, wind_scale, visibility, pressure
  → 格式化输出

多天预报:
  用户: "杭州未来五天"
  → function_call("weather", {"city":"杭州", "days":7})
  → WeatherCommand.execute(["杭州", "7"])
  → getForecastMulti("杭州", apiDays=7, showDays=5)
  → GET /v3/weather/daily.json?location=杭州&days=7
  → 解析 daily[] 数组 → 取前5天
  → 格式化: "07-18 明天: 晴 28~38°C\n07-19 后天: 多云 26~35°C\n..."

智能分流:
  "杭州多少度" / "杭州明天" / "纽约天气"
    → LLM 判断是实时/预报 → function_call(weather) → 心知天气 API（精确数据）
  "杭州热不热" / "杭州昨天天气" / "杭州夏天一般多少度"
    → LLM 判断是主观/历史/常识 → 千问直接回答（不调 API，心知查不了历史）
```

**关键点**：天数自动匹配策略。
```
days ≤ 3 → API 调用 days=3
days ≤ 7 → API 调用 days=7
其他     → API 调用 days=15
```

LLM 根据用户自然语言自动判断 days 参数：今天→1，未来两天→3，未来五天→7，未来十天→15。

---

### 6. 多图处理机制

**核心思路**：List 代替单值，支持累加和最多5张限制。

```
数据结构:
  ConcurrentHashMap<String, List<ImageEntry>> imageBuffers
  MAX_IMAGES = 5

累加流程:
  用户发图1 → addImageToBuffer(userId, bytes, "图片一是...")
     → images = [ImageEntry1]
     → "图片识别 1/5: ...可以继续发图片"

  用户发图2 → addImageToBuffer(userId, bytes, "图片二是...")
     → images = [ImageEntry1, ImageEntry2]
     → "图片识别 2/5: ...可以继续发图片"

  用户发图3 → addImageToBuffer(userId, bytes, "图片三是...")
     → images = [ImageEntry1, ImageEntry2, ImageEntry3]
     → "图片识别 3/5: ...已达到最多5张图片"

多图上下文拼接:
  buildImageContext(images, "合并这三张图"):
  "用户发送了3张图片：
   图片1：图片一是...
   图片2：图片二是...
   图片3：图片三是...

   用户说：合并这三张图"

  → CommandRouter.route() → LLM → function_call("generate_image")
  → ImageService.transformImage(3张byte[], prompt)
  → wan2.6-image 接收3张参考图 + "合并这三张图"指令 → 生成合并图

过期与溢出:
  5分钟过期 → getValidImages() 自动移除过期条目
  超过5张 → images.remove(0) 自动移除最旧的
```

**关键点**：与单图完全复用同一套逻辑，仅将 `ConcurrentHashMap<String, byte[]>` 升级为 `ConcurrentHashMap<String, List<ImageEntry>>`。LLM 上下文自动适配单图/多图描述格式。

---

### 7. Function Calling 工作原理

**核心思路**：定义工具(tools) → 传给 LLM → LLM 自行决定是否调用 → 解析调用结果 → 执行对应功能。

```
步骤1: 定义工具（CommandRouter.buildTools()）
  tools = [
    ToolFunction { name:"weather",  params:{city, days} },
    ToolFunction { name:"generate_image", params:{prompt} },
    ToolFunction { name:"help" },
    ToolFunction { name:"status" },
    ToolFunction { name:"version" }
  ]

步骤2: 传入 LLM（LlmService.chat()）
  GenerationParam.builder()
    .model("qwen-plus")
    .messages([system消息, 历史, 用户消息])
    .tools(tools)
    .build()

步骤3: LLM 返回（两种可能）
  情况A - 需要调工具:
    choices[0].finish_reason = "tool_calls"
    tool_calls[0].function.name = "weather"
    tool_calls[0].function.arguments = {"city":"杭州","days":1}

  情况B - 直接回答:
    choices[0].finish_reason = "stop"
    message.content = "你好呀，有什么可以帮你？"

步骤4: 处理结果（CommandRouter.route()）
  情况A → commands.get(fnName).execute(args) → 返回命令结果
  情况B → 直接返回文本 → ChatService.addHistory() → sendReply()
```

**关键优势**：一次 API 调用同时完成"意图判断"和"生成回复"，替代传统两阶段方案（先判断意图再生成回复），减少一半 API 延迟和成本。

---

### 8. 意图理解的演进之路

**关键词阶段**：
```
用户说"杭州天气" → 正则匹配 → 执行weather命令
用户说"天气"     → 匹配失败 → 返回"未知命令"
```
问题：只有精确匹配才生效，自然语言完全无法识别。

**关键词 + LLM分类阶段**：
```
用户说"画一只猫" → isImageGenRequest？→ 关键词匹配 → 未命中 → 聊天
用户说"p掉电脑" → isImageGenRequest？→ 关键词匹配 → 未命中 → 聊天
```
问题：keyword 列表是死的，永远覆盖不全同义表达（P图/p掉/去掉/移除/擦除...）。

**LLM Function Calling（当前方案）**：
```
用户说"把照片变卡通" → 不带关键词判断 → 直接交给 LLM + tools
  → 千问看到: "用户发送了图片，内容：{描述}。用户说：把照片变卡通"
  → 千问判断: 用户想把原图变换为卡通风格 → 调用 generate_image
  → 系统检测到图片缓存 → wan2.6-image
```
关键变化：从"我们替 LLM 判断意图"变为"LLM 自行判断意图"，减少了中间层，提高了准确率。

**演进对比**：

| 阶段 | 判读方式 | 准确率 | 维护成本 |
|------|---------|--------|---------|
| 纯关键词 | 正则/contains | 30% | 每次加新词 |
| 关键词+LLM | 关键词优先+LLM兜底 | 60% | 需要维护关键词表 |
| Function Calling | LLM全权判断 | 95% | 只需定义tools |

---

### 9. 异常处理与兜底澄清

**分层异常体系**：

```
BusinessException（业务异常基类）
  ├── MissingParameterException     // weather 没传城市
  ├── InvalidParameterException     // 参数格式错误
  ├── CityNotFoundException         // 城市不存在
  └── WeatherApiException           // 天气API调用失败
```

**各层处理策略**：

```
1. Command 层（命令执行异常）
   CommandRouter.executeCommand():
     ├── BusinessException → "[错误] " + e.getMessage()  // 友好的业务错误
     └── Exception         → "[错误] 系统异常"           // 不暴露内部细节

2. 图片处理层（图片下载/识图异常）
   handleImageMessage():
     ├── downloadImage 失败 → "抱歉，图片接收失败，请稍后再试"
     └── recognizeImage 失败 → "抱歉，图片识别失败，请稍后再试"

3. LLM 调用层（大模型API异常）
   LlmService.chat():
     ├── ApiException → "AI 服务暂时不可用，请稍后再试"
     └── 其他异常     → "AI 服务调用失败"

4. 回复层（发送异常 + 兜底）
   sendReply():
     ├── result == null/空 → "抱歉，我没理解你的意思，可以再说详细一点吗？"
     ├── IMG_GEN 失败      → "抱歉，操作失败，请稍后再试"
     └── sendText 失败     → 重试一次 "抱歉，操作失败"
```

**兜底澄清机制**：当 LLM 返回空结果或无法处理时，主动引导用户重新表达，而不是沉默或报错。

---

### 10. 对话上下文管理机制

**核心思路**：四层架构（Redis 持久化 + Token 阈值压缩 + 滑动窗口兜底 + LLM 摘要），解决长对话中上下文膨胀和丢失问题。

**架构**：

```
Layer 1: Redis 持久存储
  → chat:{userId} → JSON 历史列表，24h TTL 自动过期
  → 解决沉睡用户堆积，Redis 不可用时自动回退内存

Layer 2: Token 阈值压缩（主要防线）
  → DashScope Tokenizer 精确计算 token 数
  → 超 100K（qwen-plus 最大 128K）时触发压缩
  → 配置: llm.token-threshold: 100000

Layer 3: 滑动窗口（兜底防线）
  → 消息数超 50 条时触发压缩
  → 配置: llm.max-history: 50

Layer 4: 对话摘要（压缩手段）
  → 取最旧的一半消息 → LLM 压缩为 80 字摘要
  → 替换为 system 消息 → 保留最近 10 条完整消息
```

**压缩流程**：

```
触发条件: tokens > 100K 或 消息数 > 50
  ↓
1. 取最旧的一半消息
2. 拼接为文本 → 调 LLM 生成 80 字以内摘要
3. 替换为一条 system 消息: {"role":"system","content":"历史摘要：xxx"}
4. 保留最近 10 条完整消息 + 新摘要
5. 保存回 Redis，刷新 24h TTL
```

**示例**：

```
对话: "用户：杭州天气.. 助手：杭州28°... 用户：画只猫.. 助手：生成成功.."
摘要: "历史摘要：用户查了杭州天气(28°多云)，让画了猫"

压缩后: [摘要] + 最近10条完整消息
```

**依赖**：
| 技术 | 用途 |
|------|------|
| Redis (StringRedisTemplate) | 对话历史存储，24h TTL |
| DashScope Tokenizer | Token 精确估算 |
| LlmService.chat() | 摘要生成 |

**优势**：
- Redis 自动清理沉睡用户，零维护
- Token 精确估算，不浪费模型窗口
- 双重阈值互备，Redis 异常时回退内存
- 摘要压缩保留核心信息，丢弃冗余原文

---

### 11. 语音收发与文件模式

**核心思路**：收语音用 VoiceItem.getText()（iLink 自动转写），发语音用 TTS + sendFile(MP3)。

**语音接收**：

```
用户发语音气泡
  → extractVoice() 检测 VoiceItem
  → voiceItem.getText() → iLink SDK 自动转写文字
  → CommandRouter.route(text) → 与文字消息走同一流程
```

**语音发送**：

```
开启语音模式 → 所有文字回复自动附带 MP3 语音文件

文字回复 → VoiceService.textToSpeechMp3(text)
  → HttpSpeechSynthesizer.callAndReturnAudio()
  → cosyvoice-v3-flash → format("mp3") → byte[]
  → client.sendFile(fromUserId, bytes, "语音.mp3", "")
  → 微信文件消息 → 点开播放
```

**为什么不用语音气泡**：
- 微信气泡仅支持 SILK 格式，WAV/MP3 不显示
- 触碰语音协议有封号风险
- 改用 sendFile 文件模式，无风险，手机端点开即播

**切换控制**：
```
"开启语音" → voiceMode[userId]=true → 回复附带语音
"关闭语音" → voiceMode[userId]=false → 仅回复文字
```

---

### 12. 用户记忆机制

**核心思路**：LLM 通过 Function Calling 保存用户个人信息（名字、生日、喜好等）到 MemoryService，后续对话自动加载到 system prompt。

**保存流程**：

```
用户: "叫我托尼"
  → LLM 判断是个人信息 → function_call("remember_fact", {"key":"名字","value":"托尼"})
  → CommandRouter.handleRememberFact()
  → MemoryService.setFact(userId, "名字", "托尼")
  → 写入 memory-{userId}.json

文件内容: {"名字": "托尼", "生日": "5月20号", "职业": "程序员"}
```

**读取流程**：

```
每次 LlmService 调用时:
  LlmService.buildSystemPrompt(userId)
  → MemoryService.getFormattedFacts(userId)
  → "用户信息：名字是托尼，生日是5月20号"
  → 拼接到 system prompt → LLM 对话时自动使用
```

**数据结构**：

```
ConcurrentHashMap<String, ConcurrentHashMap<String, String>> + JSON文件
内存加速 + 文件持久化，重启不丢
```

**tool 定义**：

| 参数 | 说明 |
|------|------|
| key | 信息类别（如：名字、生日、喜好、职业） |
| value | 具体内容（如：托尼、5月20号、篮球、程序员） |
